var applykey = function (_event_) {

	var winObj = checkEventObj(_event_); // On récupère l'objet 'event' 
	var intKeyCode = winObj.keyCode; 	 // On récupère la valeur de keyCode (qui représene la touche du clavier)

	switch($intKeyCode) {
		case KEY_RIGHT:

		break;
		case KEY_LEFT:

		break;
		case KEY_DOWN:

		break;
		case KEY_UP:

		break;

		// Gestion paramètres
		case KEY_ESS:

		break;
	}
}